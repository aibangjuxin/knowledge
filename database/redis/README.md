# Redis 知识库

## 目录描述
本目录包含Redis内存数据库相关的知识、配置、连接方法和云平台集成。

## 目录结构
```
redis/
├── docs/                     # Markdown文档
└── README.md                 # 本说明文件
```

## 子目录说明 (按功能分类)
### docs/ - 文档
- `gcp-redis.md`: Google Cloud Platform Redis服务
- `get-gcp-redis.md`: 获取GCP Redis实例
- `In-transit-encryption.md`: 传输中加密相关
- `redis-connect-chatgpt.md`: 通过ChatGPT连接Redis
- `redis-connect-claude.md`: 通过Claude连接Redis
- `redis-connect-gemini.md`: 通过Gemini连接Redis
- `redis.md`: Redis基础和高级功能

## 快速检索
- Redis基础: 查看 `docs/` 目录中的 `redis.md`
- GCP集成: 查看 `docs/` 目录中的 `gcp-redis.md` 和 `get-gcp-redis.md`
- 安全配置: 查看 `docs/` 目录中的 `In-transit-encryption.md`
- AI辅助连接: 查看 `docs/` 目录中的 `redis-connect-*.md` 文件