# DNS

DNS 相关知识库（GCP DNS、域名解析等）。

## 目录结构

```
dns/
├── dns-peering/           # DNS Peering 配置
├── docs/                  # DNS 文档
└── *.drawio.png/svg       # 架构图（保留在根目录）
```

## 根目录文件

README.md, gcp-dns-forwarding.drawio.png, gke-dns-resolution.drawio.png, gke-dns-resolution.drawio.svg

## 子目录内容

- `dns-peering/`: 19 文件
- `docs/`: 34 文件
