CSI驱动程序的使用必须获得批准。<br>注意:如果CSI驱动程序与已获批准的GCP产品相关,则必须引用相应SCD中确定的控制措施。


下面我用 **GCP + GKE 实际落地** 的角度，清晰解释这条控制点在评估什么，以及你们应该如何应对。

---

# **✅ 这条要求在评估什么？**

  

原文：

  

> **CSI 驱动程序的使用必须获得批准。如果 CSI 驱动程序与已获批准的 GCP 产品相关，则必须引用相应 SCD 中确定的控制措施。**

  

这句话包含两个关键点：

---

# **🔍 ① “CSI 驱动程序（Container Storage Interface）必须经过批准”**

  

换句话说：

  

> **你不能随便在 GKE 集群中安装第三方存储插件（CSI drivers）。**

> **任何 CSI driver 的使用都必须经过安全审批。**

  

为什么？

  

CSI driver 通常拥有以下权限：

- 在 node 上读写文件系统（高风险）
    
- 管理挂载点（高权限）
    
- 使用 privileged / hostPath（高敏感）
    
- 管理 PersistentVolume（数据访问风险）
    

  

因此：

  

> **CSI driver 是高风险组件，需要安全评估。**

---

# **🔍 ② 如果 CSI driver 是 Google 官方提供的 → 可以直接使用，但需引用其官方安全控制（SCD）**

  

SCD = **Security Control Document**（Google 内的安全控制描述文档）

  

例如官方 GCP CSI driver：

|**CSI Driver**|**类型**|**状态**|
|---|---|---|
|GCE PD CSI driver|Google 官方|安全批准|
|Filestore CSI driver|Google 官方|安全批准|
|Secrets Store CSI driver（GCP secret manager provider）|Google 官方|安全批准|

如果是 Google 官方支持的产品，它们本身已经通过：

- Google 的 SRE 安全审查
    
- GCP 产品 SCD（Security Control Document）
    
- 行业合规（ISO/SOC/PCI 等）
    

  

所以：

  

> **你使用这些 GCP 官方 CSI driver，只需要引用对应的 Google SCD 作为安全控制依据即可，不需要额外审批。**

---

# **🔍 ③ 如果是第三方 CSI 驱动程序 → 必须走高风险变更或安全审批**

  

比如：

- 来自开源社区的 Rook Ceph CSI
    
- 第三方存储厂商的 CSI（NetApp、Dell EMC、Pure、MinIO）
    
- 自研 CSI
    

  

这些情况：

  

> **必须先做安全评估，确保其权限、操作、架构符合组织安全标准。**

---

# **🧩 总结一句话**

  

这条控制点的根本评估内容是：

  

> **你的 GKE 中使用的所有 CSI 驱动是否安全、是否获得审批、是否引用了官方安全控制文档。**

> **是否避免安装未经批准的第三方 CSI。**

---

# **📌 用合规审计能用的回答（英文示例）**

```
Use of CSI drivers requires prior approval due to the elevated privileges involved. 
If the CSI driver is associated with an approved GCP managed product (e.g., GCE PD CSI, Filestore CSI, Secret Manager CSI), we reference the corresponding Google SCD as the security control evidence. 
Third-party or custom CSI drivers are not allowed unless formally security-reviewed and approved.
```

---

# **📦 如果你需要，我还可以帮你列**

- 你们公司应该允许的“已批准 GCP CSI 列表”
    
- 哪些高风险 CSI 不应该使用
    
- 一份完整的“CSI 驱动审批流程（流程图 + 文档模板）”
    

  

需要吗？