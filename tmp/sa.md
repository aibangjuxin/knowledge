对于GCP平台上的一个Firewall，我想做一些深度的探索。
比如说我知道，对于GCE来说，不同的GCE，比如说我称之为为不同的components 比如nginx squid 
对于这种不同的conference，我们肯定是运行在了不同的service Account 
eg:
abjx-nginx@projectid.iam.gserviceaccount.com
abjx-squid@projectid.iam.gserviceaccount.com
目前知道的情况是，如果我想让这两个组件通信的话，必须有流出和对应的流入规则，也就是对应的Egress和Ingress。
我知道基于service account可以做对应的Ingress规则。
假如说我允许Nginx访问Squid，流进到Squid

对于防火墙而言，我需要Nginx配置一个Egress。然后对于Squid来说，我需要一个Ingress。

不知道这个Nginx的Egress规则。是不是也可以基于Service Account来对应配置？这个是我需要探索的地方。
以往的情况下，我都是允许流出对应的IP地址，也就是IP范围。这样的话可能就不能解耦。就说假如我IP改变了，那我就需要去调整对应的防火墙规则，这种不是我期待的。我想了解，随着技术的发展，现在有没有好的一些实现方式来解决类似这样的问题？
这样的话，比如说我在多平台的情况下，我要做一个统一的架构设计的时候，可能我只关心这个对应的名称就可以了。即使在我IP范围改变的情况下，我也不需要去动我原来的Firewall规则的名称之类的。因为最终这个Firewall的规则是要add-tags到我对应的Instance主机的。


另外还有一种情况，就比如说我的GCE是运行在一个VPC上面。但是我的GKE是运行在VPC下面的子网上面的。我还要确保这两个能够通讯的情况下。就是说GKE本身会有一些Node节点的概念，还有一些Pod IP的概念，还有一些Service IP的概念等等。我我如何基于Ingress和Egress去做对应的规则，才能让它们之间通讯？我如何基于Ingress和Egress去做对应的规则才能让它们之间通讯？

还有另外一种情况，就是说我的GKE里边的Pod。如果要Egress出去的话，我如果配置了一个对应的三层代理，也就是一个GCE的Instance主机作为它的代理当然了，这个GCE的instance我配置的是nat 就是典型的iptables 可能会允许来源是Node的IP range，还有Pod的IP range等等。这种情况下，我怎么去让我的网络上面能够是正常工作的？需要增加哪些对应的Firewall Tag或者哪些对应的防火墙规则？这个也是我需要去考虑和评估的。


